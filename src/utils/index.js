const createFinalMediaFile = require('./createFinalMediaFile');
const handleRange = require('./handleRange.js');
const createFileName = require('./createFileName');

module.exports = {
  createFinalMediaFile,
  handleRange,
  createFileName
};
